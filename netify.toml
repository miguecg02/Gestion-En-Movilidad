[build]
  command = "npm run build"  # Ejecuta desde raíz
  publish = "dist"           # Directorio de salida de Vite
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true  # Fuerza SPA mode

[context.production.environment]
  NODE_VERSION = "18"
  VITE_API_URL = "https://tu-backend-real.com"  # Reemplaza con tu URL real